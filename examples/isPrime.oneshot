LET N = 7

LET ISPRIME = TRUE

FOR (LET I = 2 : I < SQRT(N) : I = I + 1) THEN
    IF (N % I == 0) THEN
        ISPRIME = FALSE
    END
END

IF (ISPRIME) THEN
    PRINT N + " IS PRIME"
ELSE 
    PRINT N + " IS NOT PRIME"
END